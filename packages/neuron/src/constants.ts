/** Default knobs used by the neuron runtime when callers omit explicit config. */
export const DEFAULT_RETRIES_HARD_LIMIT = 30;
export const DEFAULT_MAX_CONNECTION_RETRIES = 10;

export const DEFAULT_CRITIC_STRATEGY = "strict";

export const DEFAULT_LEARNING_EXAMPLES = 5;
export const DEFAULT_LEARNING_ONLY_FAILED_FIELDS = true;
